<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nos Groupes</title>
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    <style>
        /* Style pour uniformiser les images */
        .groupe-img img {
            width: 100%;
            height: 250px; /* Hauteur fixe */
            object-fit: cover; /* Ajuste l'image sans déformation */
            border-radius: 10px 10px 0 0;
        }
        
        .groupe-item {
            border: 1px solid #ddd;
            border-radius: 10px;
            overflow: hidden;
            background: #fff;
            transition: transform 0.3s ease-in-out;
        }

        .groupe-item:hover {
            transform: translateY(-5px);
        }
    </style>
</head>
<body>

<div class="container-fluid groupes pb-5">
    <div class="container pb-5">
        <div class="text-center mx-auto pb-5 wow fadeInUp" data-wow-delay="0.2s" style="max-width: 800px;">
            <h4 class="text-primary">Nos Groupes</h4>
            <h1 class="display-5 mb-4">Rejoignez nos communautés sociales</h1>
            <p class="mb-0">Partagez vos passions et échangez avec d'autres membres au sein de nos groupes thématiques.</p>
        </div>
        <div class="row g-4">
            {% set groupes = [
                {'image': 'grp-art.jpg', 'title': 'Art'},
                {'image': 'grp-music.jpg', 'title': 'Musique'},
                {'image': 'grp-ciema.jpg', 'title': 'Cinéma'},
                {'image': 'grp-theatre.jpg', 'title': 'Théâtre'},
                {'image': 'grp-arch.jpg', 'title': 'Architecture'},
                {'image': 'grp-dance.jpg', 'title': 'Danse'}
            ] %}

            {% for groupe in groupes %}
                <div class="col-md-6 col-lg-4 wow fadeInUp" data-wow-delay="{{ loop.index * 0.2 }}s">
                    <div class="groupe-item">
                        <div class="groupe-img">
                            <img src="{{ asset('/img/' ~ groupe.image) }}" class="img-fluid" alt="Image de {{ groupe.title }}">
                        </div>
                        <div class="rounded-bottom p-4">
                            <a href="#" class="h4 d-inline-block mb-4">{{ groupe.title }}</a>
                            <p class="mb-4">Rejoignez le groupe {{ groupe.title }} pour échanger avec d'autres passionnés et partager vos expériences.</p>
                            <a class="btn btn-primary rounded-pill py-2 px-4" href="#">Découvrir</a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</div>

</body>
</html>
