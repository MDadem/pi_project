<div class="container-fluid position-relative p-0">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark px-4 px-lg-5 py-3 py-lg-0">
        <a href="" class="navbar-brand p-0">
            <h1 class="text-primary m-0"></i>Culture Space</h1>
            <!--  -->
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
            <span class="fa fa-bars"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <div class="navbar-nav ms-auto py-0">
                <a href="{{ path('app_home') }}" class="nav-item nav-link text-light {% if app.request.get('_route') == 'app_home' %}active{% endif %}">Home</a>

                {# <a href="{{ path('app_events') }}" class="nav-item nav-link text-light {% if app.request.get('_route') starts with 'app_event' %}active{% endif %}">Events</a>                <div class="nav-item dropdown"> #}
                  
                </div>
                {# <a href="{{ path('app_contact') }}" class="nav-item nav-link text-light {% if app.request.get('_route') == 'app_contact' %}active{% endif %}">Contact Us</a> #}
            </div>
            <a href="#" class="btn btn-primary rounded-pill py-2 px-4 my-3 my-lg-0 flex-shrink-0">Get Started</a>
        </div>
    </nav>

</div>