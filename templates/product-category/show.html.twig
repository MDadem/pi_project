<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Category Details | CultureSpace</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet">
    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&family=Roboto:wght@400;500;700;900&display=swap" rel="stylesheet">

    <!-- Icon Font Stylesheet -->
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.4/css/all.css"/>

    <!-- Libraries Stylesheet -->
    <link rel="stylesheet" href="{{ asset('lib/animate/animate.min.css') }}"/>

    <style>
        body {
            background: linear-gradient(135deg, #e8f5e9 0%, #c8e6c9 100%);
            min-height: 100vh;
            font-family: 'Inter', sans-serif;
        }

        .container {
            max-width: 900px;
            margin-top: 4rem;
            margin-bottom: 4rem;
        }

        .card {
            border: none;
            border-radius: 20px;
            background: rgba(255, 255, 255, 0.95);
            box-shadow: 0 10px 50px rgba(0,0,0,0.15);
            transition: all 0.4s ease;
            overflow: hidden;
            position: relative;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 60px rgba(0,0,0,0.2);
        }

        .card:before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(40,167,69,0.1) 0%, transparent 70%);
            transform: rotate(30deg);
            pointer-events: none;
        }

        .card-header {
            border-radius: 20px 20px 0 0;
            padding: 2rem;
            background: linear-gradient(45deg, #28a745, #34d058);
            position: relative;
            z-index: 1;
        }

        .card-header h2 {
            color: white;
            font-weight: 800;
            font-size: 2.5rem;
            margin: 0;
            text-shadow: 0 2px 10px rgba(0,0,0,0.2);
        }

        .card-header:after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 2rem;
            width: 80px;
            height: 4px;
            background: linear-gradient(to right, #ffffff, rgba(255,255,255,0.5));
            border-radius: 4px;
        }

        .card-body {
            padding: 3rem;
            background: white;
            border-radius: 0 0 20px 20px;
            position: relative;
            z-index: 1;
        }

        .card-body p {
            margin-bottom: 1.5rem;
            font-size: 1.1rem;
            color: #34495e;
        }

        .card-body p strong {
            color: #2c3e50;
            font-weight: 700;
            margin-right: 0.75rem;
            display: inline-block;
            min-width: 100px;
        }

        .card-body .text-muted {
            color: #7f8c8d !important;
            font-style: italic;
        }

        .btn {
            border-radius: 30px;
            padding: 0.75rem 2rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            border: none;
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }

        .btn:before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            background: rgba(255,255,255,0.2);
            border-radius: 50%;
            transform: translate(-50%, -50%);
            transition: width 0.6s ease, height 0.6s ease;
        }

        .btn:hover:before {
            width: 300px;
            height: 300px;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.2);
        }

        .btn-warning {
            background: linear-gradient(45deg, #ffb347, #ffcc33);
        }

        .btn-danger {
            background: linear-gradient(45deg, #ff416c, #ff4b2b);
        }

        .btn-secondary {
            background: linear-gradient(45deg, #bdc3c7, #2c3e50);
        }

        .alert {
            border-radius: 12px;
            padding: 2rem;
            background: rgba(255, 255, 255, 0.9);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            animation: slideIn 0.5s ease;
            border-left: 5px solid #28a745;
            backdrop-filter: blur(2px);
        }

        .alert-heading {
            color: #28a745;
            font-weight: 700;
            margin-bottom: 1rem;
        }

        @keyframes slideIn {
            from { opacity: 0; transform: translateY(-30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .d-flex.gap-2 {
            gap: 1rem !important;
        }
    </style>
</head>
<body>
    {% include 'frontend/navbar/navbar.html.twig' %}

    {% block body %}
        <div class="container mt-5 mb-5">
            {% for productCategory in productCategories %}
                <div class="card shadow-lg mb-4 wow fadeInUp" data-wow-delay="0.2s">
                    <div class="card-header text-white">
                        <h2 class="mb-0 fw-bold">Category Details</h2>
                    </div>
                    <div class="card-body">
                        <div class="mb-4">
                            <p class="lead"><strong><i class="fas fa-id-badge me-2"></i>ID:</strong> {{ productCategory.id }}</p>
                            <p class="lead"><strong><i class="fas fa-tag me-2"></i>Name:</strong> {{ productCategory.name }}</p>
                            <p class="text-muted"><strong><i class="fas fa-align-left me-2"></i>Description:</strong> {{ productCategory.description }}</p>
                        </div>
                        
                        <div class="d-flex gap-2">
                            <a href="{{ path('product_category_edit', {'id': productCategory.id}) }}" 
                               class="btn btn-warning fw-semibold">Edit</a>
                            
                            <form method="post" 
                                  action="{{ path('product_category_delete', {'id': productCategory.id}) }}" 
                                  style="display:inline-block;" 
                                  onsubmit="return confirm('Are you sure you want to delete this category?');">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ productCategory.id) }}">
                                <button class="btn btn-danger fw-semibold">Delete</button>
                            </form>
                            
                            <a href="{{ path('product_category_index') }}" 
                               class="btn btn-secondary fw-semibold">Back to List</a>
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="alert alert-info text-center mt-4 wow fadeIn" data-wow-delay="0.2s" role="alert">
                    <h4 class="alert-heading"><i class="fas fa-info-circle me-2"></i>No Categories Found</h4>
                    <p>It seems there are no categories to display at the moment.</p>
                </div>
            {% endfor %}
        </div>
    {% endblock %}

    {% include 'frontend/footer/footer.html.twig' %}
</body>
</html>